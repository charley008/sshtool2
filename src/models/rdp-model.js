// Alias for rdp
// Recovered module id: 86
"use strict";

class RDP {
    constructor(isFullScreen, port, colorDepth, desktopGeometry) {
        this.isFullScreen = isFullScreen;
        this.port = port;
        this.colorDepth = colorDepth;
        this.desktopGeometry = desktopGeometry;
    }
}
exports.RDP = RDP;
